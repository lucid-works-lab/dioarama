import {Label, Navbar, NavItem, NavDropdown, Nav, MenuItem, PageHeader, Col} from 'react-bootstrap'
import React, {Component, PropTypes} from 'react'

class TestRunnerPage extends Component {
    render () {
        return (
            <div>
                TEST RUNNER
            </div>
        )
    }
}

export default TestRunnerPage;